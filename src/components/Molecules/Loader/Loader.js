import Loading from 'components/Atoms/Loading';
import { StyleWrapper } from './styled';

const Loader = () => {
  return (
    <StyleWrapper>
      <Loading />
    </StyleWrapper>
  );
};

export default Loader;
